<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Necessario para o app acessar rede no build release (o Flutter so
         adiciona INTERNET automaticamente em debug/profile). -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Agendamento de ajuste ("erguer para 120 F as 14h"). O alarme exato e o
         que faz o aviso sair na hora marcada mesmo com o celular em repouso
         (Doze); sem ele o Android agrupa e pode atrasar bastante. USE_EXACT_ALARM
         nao precisa de aprovacao do usuario e vale para app de alarme, que e
         exatamente o caso aqui. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <!-- Reiniciar o celular apaga os alarmes do sistema. O receptor do plugin
         reagenda o que ficou pendente; o app tambem reagenda ao abrir, porque
         nem todo fabricante entrega o BOOT_COMPLETED de forma confiavel. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <application
        android:label="Sentinela Smart"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- O convite por QR. Quem recebe aponta a CAMERA NORMAL do celular
                 para o codigo: ele carrega `sentinela://convite?c=...`, e e este
                 filtro que faz o Android oferecer o Sentinela para abri-lo.
                 Sem leitor de QR embutido, e portanto sem permissao de camera.

                 BROWSABLE e obrigatorio - e a categoria que a camera (e o
                 navegador) usam ao entregar um endereco. Sem ela o filtro
                 existe e nunca casa: falha calada, que so aparece com o QR na
                 mao. O host restringe ao nosso assunto, para o app nao se
                 oferecer para qualquer `sentinela://`.

                 A activity ja e `singleTop`: com o app aberto, o convite chega
                 por `onNewIntent` na mesma tela, sem reiniciar o app. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="sentinela" android:host="convite"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="sentinela_alertas" />
        <!-- Entrega o aviso agendado ("erguer para 120 F") na hora marcada. O
             plugin nao declara os proprios receptores, e sem eles o
             agendamento e aceito e nunca dispara - falha calada. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <!-- Reagenda o que estava pendente depois de reiniciar o celular. As
             acoes extras cobrem fabricantes que usam o proprio nome de boot em
             vez do BOOT_COMPLETED padrao. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
