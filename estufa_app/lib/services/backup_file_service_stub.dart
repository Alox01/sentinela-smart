Future<String> salvarBackupJsonImpl({
  required String fileNameBase,
  required String jsonContent,
}) async {
  throw UnsupportedError(
    'Exportação de backup não suportada nesta plataforma.',
  );
}
